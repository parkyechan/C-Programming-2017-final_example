#include<cstdio>
float sum(float x, float y);
float cha(float x, float y);
float gob(float x, float y);
float divide(float x, float y);
int main(){
  float num1, num2, sum_result, cha_result, gob_result, divide_result;
  for(;;){
    printf("%s\n", );
  }

  // 길어서 생략. 강의 21분 정도에 있음.
}
